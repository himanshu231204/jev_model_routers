# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This repository's project instructions live in `AGENTS.md`, not here — read that file first.
It covers commands (install/test/single-test/real-Jev test), module boundaries, routing
invariants, testing rules and the definition of done for `src/jev_router_live/`. `AGENTS.md`
is kept up to date with `src/` and `tests/`; this file exists only so tooling that looks for
`CLAUDE.md` specifically finds a pointer to it.
